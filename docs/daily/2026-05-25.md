# 2026-05-25

## 会话摘要
继续收敛月面场景，移除剩余装饰树与背景贴图层，并把整体环境色调压到单一墨夜配置。

## 记录
- [决策] 在 [assets/SceneRoot-DLjunhxr.js](/Volumes/SSD/code/hyacinth.im-site/assets/SceneRoot-DLjunhxr.js) 中直接禁用 `ae()`、`oe()`、`$e()`，不再显示远景山体、云层贴片和中心区域装饰树。
- [决策] 将 `tt` 天气配置收敛为单一 `night / 墨夜` 主题，背景色 `#05070b`，雾色 `#0a0d12`，避免保留白天/黄昏等旧视觉分支。
- [决策] 将喷泉 `He()` 与小电台 `We()` 直接空组化，从场景里彻底移除，不再保留模型、交互底座和局部粒子层。
- [决策] 将通用模型渲染器 `Y()` 改为“基础常亮发光 + 激活增强”，并单独提高帐篷 `circus-tent.glb` 的 `autoGlowIntensity` 与 `highlightIntensity`。
- [进展] 玩家主体 `cape-cat.glb` 已在 `Te()` 的材质遍历里注入默认 emissive，进入场景即自发光。
- [决策] 将上一版“靠 emissive 刷亮表面”的方案收回到弱辅助层，给玩家主体和中央帐篷各自挂上真实 `pointLight`，让地面和周边模型实际吃光。
- [决策] 新增统一亮度真相源 `window.__MY_MOON_LIGHTING__`，分成 `centerpiece`、`small`、`player` 三组参数，分别控制中央大模型、其他小模型和用户操作主体。
- [进展] `Y()`、`Te()`、`Be()`、`Ve()`、`qe()` 已全部切到统一亮度配置，不再各自写死亮度参数。
- [进展] 在 AI 覆盖层中新增三组亮度滑杆，直接调 `window.__MY_MOON_LIGHTING__.*.brightness`，并由场景逐帧读取，实时生效。
- [决策] 新增 `assets/my-moon-runtime-config.js` 作为可持久化运行时配置，入口 HTML 在主体 bundle 前加载它。
- [决策] 新增 `tools/my-moon-config-server.mjs`，提供静态预览与 `POST /__my-moon/save-config`，让 Web 控制台保存后真实写回配置文件。
- [进展] 模型控制台已扩展为三组参数：中间大模型、其他小模型、用户操作主体，分别支持亮度、颜色和缩放。
- [经验] “保存后更新代码”不能靠普通静态服务器完成；需要本地写回服务，否则浏览器只能落到 `localStorage`。
- [经验] 向压缩 bundle 注入 helper 时不能再用 `ce` 这类短名；原构建产物已存在同名函数，导致静态作用域重定义并直接打断场景执行。当前已改为唯一前缀 `moonLightingProfile`。
- [经验] 压缩 bundle 的单个回调里存在局部短名 `u`，不能在该回调前段使用 `new u(...)`；会被后续 `let u = ...` 遮蔽并触发 TDZ。当前玩家材质颜色改为 `emissive.set(hexString)`。
- [经验] 月面这种高反差场景里，单纯提高 emissive 会把浅色模型直接刷白；要保留体积感，必须把可见亮度拆成“弱自发光 + 独立局部光源”。
- [经验] 这个项目的真实场景入口仍是构建产物 `assets/SceneRoot-DLjunhxr.js`；`docs/recovered/SceneRoot.recovered.jsx` 只负责补丁定位和语义镜像，修改它不会影响运行时。
- [进展] 已同步更新 recovered 文档，新增 `DisabledBackdropMountains`、`DisabledBackdropClouds`、`DisabledDecorativeTree` 与夜景天气配置说明。
- [修复] 单一 `night` 天气配置下，原定时切换会把当前 key 过滤成空数组，180 秒后返回 `undefined` 并触发 `background` 白屏；当前 `it()` 已加 `tt[0]` 回退。
- [决策] 中央大模型和其他小模型默认关闭材质自发光，改由各自 `pointLight` 发光；`small.lightIntensity` 从 `0` 提到 `1.2`，避免小模型仍停留在表面着色逻辑。
- [修复] 模型控制台右侧面板新增内部滚动，避免亮度、颜色、缩放控件被底部遥测条遮挡。
- [修复] 本地写回服务同时支持 `/__my-moon/save-config` 与 `/my-moon/save-config`，兼容旧缓存脚本发出的保存请求，避免 405。
- [决策] 将非用户小模型拆成独立调参 key：`shop` 控制合作小铺/种植仓，`guide` 控制向导尼卡，保留 `small` 作为其他 GLB 节点 fallback。
- [修复] 通用模型渲染器 `Y()` 将 GLB 缩放组与 `pointLight` 拆成兄弟节点，避免中间大模型 `scale:15` 把光源位置一并放大到远处。
- [进展] AI 模型控制台新增“合作小铺 / 种植仓”和“向导尼卡”两组独立亮度、颜色、缩放控件。
- [进展] 向导尼卡顶部已补 `world-label` 文本卡片，与其他空间节点的提示方式一致。

## Retain
- → bank/experience: 继续做视觉清场时，优先禁用构建产物中的挂载点和整层函数，比逐个替换模型/贴图更稳。
- → bank/experience: 构建产物只剩单一配置项时，仍要检查定时轮换/随机选择逻辑是否会过滤成空集合。
- → bank/experience: Three.js 中点光源不要挂在被大比例缩放的模型组内，否则光源位置会被缩放变换推远，表现为“模型有配置但现场没光”。
