# 2026-05-24

## 会话摘要
将用户提供的 `assets/textures/plaza-ground.png` 接入原 3D 个人主页主体场景，替换底部主地表材质。

## 记录
- [决策] 不再使用外部月面 terrain canvas 覆盖层，改为直接修改 `assets/SceneRoot-DLjunhxr.js` 的原生 Three/R3F 地面材质，避免覆盖层穿模。
- [进展] `SceneRoot-DLjunhxr.js` 已在真实可见地表入口 `Ye()` 与 `Je()` 直接加载 `assets/textures/plaza-ground.png`，覆盖原 `plaza-ground.webp` 外圈地表与 `cross-platform-ice-blue.webp` 中心地表。
- [进展] 新增 `docs/recovered/SceneRoot.recovered.jsx`，记录从压缩场景 chunk 反整理出的地表组件骨架；该文件不参与构建，只服务后续定位。
- [进展] 禁用 `Xe()` 的 42 个五边形铺片层，让月面贴图保持连续，不再出现浅色多边形覆盖块。
- [验证] `node --check assets/SceneRoot-DLjunhxr.js` 通过；静态服务器对 `assets/textures/plaza-ground.png` 返回 `200 OK`；浏览器插件当前不可用，未完成截图级验证。

## Retain
- -> bank/experience: 静态构建产物仓库中若原场景地表由 bundle 程序化生成，替换单个纹理文件不会生效；应先定位真实渲染组件，例如本轮真实入口是 `Ye()` 与 `Je()`，不是背景层 `ie()`。
- -> bank/world: 当前“我的月球”主入口继续沿用原 3D 前端，AI 系统只作为覆盖层；地表纹理已由 `SceneRoot-DLjunhxr.js` 的 `Ye()` / `Je()` 直接引用 `assets/textures/plaza-ground.png`。
- -> bank/experience: 原场景地表的浅色五边形来自 `Xe()` 中 `circleGeometry args:[.9+t%4*.13,5]` 的程序化铺片层；月面替换时应禁用该层而不是改贴图。
